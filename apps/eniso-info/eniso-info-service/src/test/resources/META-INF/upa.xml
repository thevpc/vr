<?xml version="1.0" encoding="UTF-8"?>
<upa xmlns="http://github.com/thevpc/upa/upa-1.0.xsd"  version="1.0">
    <scan types="net.vpc.app.**"/>
    <include>
        <!--<file failSafe="true" path="${user.home}/.config/eniso.info/upa.xml"/>-->
        <!--<file path="${user.home}/workspace/eniso.info/config/upa.xml"/>-->
        <!-- this is a fallback if no 'failSafe' config is loadable -->
        <default>
            <persistenceUnit name="main">
                <connection>
                    <connectionString>
                        derby:embedded://runtime/derby-enisoinfo;structure=create;userName=enisoinfo;password=enisoinfo
                    </connectionString>
                </connection>
            </persistenceUnit>
        </default>

    </include>
</upa>
